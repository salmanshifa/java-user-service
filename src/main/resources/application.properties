spring.datasource.url=jdbc:mysql://localhost:3306/java_users?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Mohd@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Logging configuration for file-based tracking
logging.level.root=INFO
logging.level.org.example=INFO

# Cron expression for the scheduled user count job
app.cron.user-count=0 * * * * *

# JWT configuration
jwt.secret=change-me-please-in-production
jwt.expiration-ms=3600000
